server {
    listen 80;
    server_name cont.uley.su;

    location = /favicon.ico { access_log off; log_not_found off; }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/thoth.sock;
    }
}
